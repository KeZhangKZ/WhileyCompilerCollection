package wycc;

import java.util.List;

import wycc.lang.PluginContext;

public class Application extends Main {
	private final PluginContext context;
	
	public Application(PluginContext context) {
		this.context = context;
	}
	
	public void run(List<String> args) {
		
		// What this will do is actually implement the default "application".
		
		// It will need to create options for defining where different kinds of
		// files go.
		
		// It will need to create build rules for all different possibilities.
		
	}
}
